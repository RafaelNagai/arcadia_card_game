{
  "name": "eltyca",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspace=packages/engine",
    "test:watch": "npm run test:watch --workspace=packages/engine",
    "simulate": "npm run simulate --workspace=packages/engine",
    "dev:web": "npm run dev --workspace=packages/web"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
